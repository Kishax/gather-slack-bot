version: 1.0
runtime: nodejs18
build:
  commands:
    # ビルドフェーズの前に依存関係をインストールします
    pre-build:
      - npm install
run:
  # package.jsonのstartスクリプトを実行します
  command: npm start
  network:
    # アプリケーションがリッスンするポート番号
    port: 3000
  env:
    - name: NODE_ENV
      value: production
    - name: AWS_REGION
      value: ap-northeast-1
  secrets:
    # 環境変数名: Secrets ManagerのARN:JSONキー
    GATHER_API_KEY: arn:aws:secretsmanager:ap-northeast-1:126112056177:secret:gather-bot-apprunner-secrets-EuGLYZ:GATHER_API_KEY
    GATHER_SPACE_ID: arn:aws:secretsmanager:ap-northeast-1:126112056177:secret:gather-bot-apprunner-secrets-EuGLYZ:GATHER_SPACE_ID
    SLACK_WEBHOOK_URL: arn:aws:secretsmanager:ap-northeast-1:126112056177:secret:gather-bot-apprunner-secrets-EuGLYZ:SLACK_WEBHOOK_URL
